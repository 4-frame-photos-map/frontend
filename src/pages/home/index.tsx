import NavBar from '@components/common/NavBar';
import PageLayout from '@components/common/PageLayout';

const Home = () => {
  return (
    <PageLayout>
      <NavBar title={'지도 지역명'} />
      Home
    </PageLayout>
  );
};

export default Home;
